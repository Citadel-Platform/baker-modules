# Conduit instrumentation

Wires a client app to Conduit: the replay capture surface, named
attention regions, the feedback widget and the consent gate.

Consent is a gate rather than a flag. In gated mode nothing is collected until
somebody agrees, with one exception — feedback a person deliberately submitted,
because refusing to send what they chose to send would be a different kind of
wrong.
